using System;

namespace FirstDemo
{
    class Program
    {
        static void Main(string[] args)
        {
          // > - Greater Than
          // < - Less Than
          // >= - Greater than or Equal too
          // <= - Less than or Equal too
          // == -  Equal To
          // != - Not Equal To

          // && - And (True if both are true)
          // || - Or (True if one is true)
        
        // Example 1
          //Console.WriteLine(5 < 10);
        
        // Example 2
          // Console.WriteLine( 4 > 2 && (5 * 4 == 22 || 8 / 3 == 2));
          // 4 > 2 && True
          // True && True
          // True

        // Example 3
          // int age;

          // Console.WriteLine("Please enter your age: ");
          // age = int.Parse(Console.ReadLine());

          // if (age >= 18)
          {
            // Console.WriteLine("You are legally allowed to gample in alberta");
          }

          // else
          {
            // Console.WriteLine("Sorry, please come back next year to try again");
          }
        // Example 4
        // string password;

        // Console.WriteLine("Please enter your password: ");
        // password = Console.ReadLine();

        // if (password == "Hello123")
        {
            // Console.WriteLine("Your password is correct!");
        }

        }
    }
}
